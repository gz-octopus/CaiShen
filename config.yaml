sqlite:
  drivername: sqlite
  database: stock_test.db
postgresql:
  drivername: postgresql+psycopg2
  host: ${PG_HOST:?请设置环境变量 PG_HOST（数据库主机地址）}
  port: ${PG_PORT:-5433}
  username: ${PG_USERNAME:?请设置环境变量 PG_USERNAME（数据库用户名）}
  password: ${PG_PASSWORD:?请设置环境变量 PG_PASSWORD（数据库密码）}
  database: ${PG_DATABASE:?请设置环境变量 PG_DATABASE（数据库名称）}

slb:
  base_dir: ${SLB_HISTORY:?请设置环境变量 SLB_HISTORY（扫雷宝历史数据目录）}
  user_diy_data: userdiydata_802.txt
  user_diy_data_by_date: userdiydata_801_{date}.txt

tdx:
  base_dir: D:/finance_tool_/new_tdx

tushare:
  token: ${TUSHARE_TOKEN}

firecrawler:
  api_key: ${FIRECRAWLER_API_KEY}

mootdx:
  # ip: 112.74.214.43
  ip: sztdx.gtjas.com
  port: 7709

data_source: tdx

# 银河证券.星耀数智
amazing_data:
  host: ${AD_HOST:-101.230.159.234}
  port: ${AD_PORT:-8600}
  username: ${AD_USERNAME}
  password: ${AD_PASSWORD}

# 博查
bocha:
  api_key: ${BOCHA_API_KEY}

# TickFlow
tickflow:
  api_key: ${TF_API_KEY}

# GS Quant
gs_quant:
  api_key: ${GS_QUANT_API_KEY}
